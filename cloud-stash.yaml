enabled: true
disable_admin: true
stashes:
  AWS:
    region: ''                  # AWS BUCKET REGION
    key: ''                     # AWS KEY
    secret: ''                  # AWS PASSWORD
    # domain:
    # defaults:
    #  target: 'fave-bucket'
